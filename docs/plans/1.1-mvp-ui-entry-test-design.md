# MVP前端核心循环UI入口 — 测试设计

## 现有测试基线

- MVP-19.x: 登录/注册/审批/路由守卫（已有，不变）
- MVP-20.1~20.6: 三栏工作区交互（已有，需适配新组件）
- E2E: 目前为空（仅有 `__init__.py`）

新增功能用与实施步骤匹配的编号（MVP-UI-4.x~MVP-UI-9.x），MVP-20.x 随组件替代更新。

---

## 步骤1：用户列表与搜索API — pytest集成测试

### 正常路径

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-1.1 | 全量用户列表 | `GET /auth/users` 返回所有 active 用户，不含 pending/rejected |
| MVP-UI-1.2 | 前缀搜索 | `GET /auth/users/search?username=ali` 返回 username 以 "ali" 开头的 active 用户 |
| MVP-UI-1.3 | 无匹配搜索 | `GET /auth/users/search?username=zzz` 返回 `[]` |
| MVP-UI-1.4 | 搜索大小写不敏感 | `GET /auth/users/search?username=ALI` 与 `ali` 结果相同 |

### 异常场景

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-1.5 | 未认证访问全量列表 | 无 Authorization header → 401 |
| MVP-UI-1.6 | 未认证访问搜索 | 无 Authorization → 401 |
| MVP-UI-1.7 | 搜索参数缺失 | `GET /auth/users/search`（无 username） → 400 |
| MVP-UI-1.8 | 空字符串搜索 | `GET /auth/users/search?username=` → 400 或返回空列表 |

测试文件：`tests/integration/test_auth_api.py` 新增 `TestUserListSearch`

---

## 步骤2：shadcn组件+resizable依赖 — 安装验证

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-2.1 | npm包安装验证 | package.json 含 react-resizable-panels、react-diff-viewer-continued；ui组件文件存在 |
| MVP-UI-2.2 | 现有测试不被破坏 | `npm run test` 全部通过 |

---

## 步骤3：类型定义+apiGet扩展+斜杠命令+hook扩展 — Vitest

### apiGet扩展

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-3.1 | apiGet带params | `apiGet('/auth/users/search', { username: 'ali' })` → URL含 `?username=ali` |
| MVP-UI-3.2 | apiGet无params向后兼容 | `apiGet('/projects')` → URL不含查询参数 |
| MVP-UI-3.3 | apiGet params类型安全 | params传入非对象 → TypeScript编译报错（通过 `tsc --noEmit` 验证，非Vitest运行时测试） |

### 斜杠命令

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-3.4 | /summarize解析 | `parseMessage('/summarize')` → `{ request_summary: true, content: '请总结当前讨论要点' }` |
| MVP-UI-3.5 | /summarize带消息 | `parseMessage('/summarize 这是观点')` → `{ request_summary: true, content: '这是观点' }` |
| MVP-UI-3.6 | /help解析 | `parseMessage('/help')` → `{ show_help: true }` |
| MVP-UI-3.7 | 普通消息不触发斜杠 | `parseMessage('你好')` → `{ request_summary: false, content: '你好' }` |
| MVP-UI-3.8 | 未知斜杠命令 | `parseMessage('/unknown')` → 作为普通消息（斜杠必须在开头才触发） |
| MVP-UI-3.9 | 斜杠在消息中间 | `parseMessage('你好 /summarize')` → 普通消息（非开头） |

### hook回调

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-3.10 | 新回调不影响initialState | 注入 initialState 后调用 handleCreateProject → 初始值不变 |

测试文件：
- `web/src/__tests__/api.test.ts` 新增 params 测试
- `web/src/__tests__/slashCommands.test.ts` 新增
- `web/src/__tests__/Workspace.test.tsx` 新增 MVP-UI-3.10

---

## 步骤4：ProjectTree组件 — Vitest

### 正常路径

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-4.1 | 两层树形渲染 | 项目节点展开显示子线程；项目节点旁图标按钮 ➕👤🤖 存在；导航栏顶部 ➕ 新建项目按钮存在 |
| MVP-UI-4.2 | 选中项目高亮 | 点击项目 → `data-selected="true"` |
| MVP-UI-4.3 | 点击项目选中默认线程 | 点击项目 → `onSelectThread` 收到默认线程ID |
| MVP-UI-4.4 | 点击子线程 | 点击线程 → `onSelectThread` 收到线程ID |
| MVP-UI-4.5 | 未读Badge | unread_count > 0 → 蓝色数字Badge |
| MVP-UI-4.6 | 选中线程不显示未读 | 当前选中线程 → 无未读提示 |
| MVP-UI-4.7 | 项目节点图标按钮回调 | ➕→onCreateThread；👤→onAddMember；🤖→onRegisterAgent |
| MVP-UI-4.8 | 顶部新建项目按钮 | 导航栏顶部➕按钮 → `onCreateProject` |

### 异常场景

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-4.9 | 项目无子线程 | 只有默认线程 → 不显示子节点展开 |
| MVP-UI-4.10 | 空项目列表 | `projects=[]` → 显示空状态提示 |
| MVP-UI-4.11 | Tooltip hover | hover图标按钮 → 显示对应文字 |

### MVP-20迁移

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-20.1-更新 | ThreadList→ProjectTree | has_summary/pending_plan/message_count 在新组件中仍正确显示 |

---

## 步骤5：4个Dialog组件 — Vitest

### CreateProjectDialog

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-5.1 | 正常创建（含自动创建默认线程） | 输入名称+描述 → 提交 → `onCreateProject` 收到 `{ name, description }`；回调内部自动调用创建线程API → `onSelectThread` 收到新默认线程ID |
| MVP-UI-5.2 | 自动创建默认线程失败 | 创建项目成功 → 创建默认线程API失败 → 项目出现但无默认线程选中，显示错误提示 |
| MVP-UI-5.3 | 名称必填 | 不输入名称 → 提交 disabled 或验证错误 |
| MVP-UI-5.4 | 取消关闭 | 取消 → Dialog关闭，回调未触发 |
| MVP-UI-5.5 | 描述可选 | 只输入名称 → 提交成功，description=null |

### CreateThreadDialog

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-5.6 | 正常创建 | 输入标题 → 提交 → `onCreateThread` 回调 |
| MVP-UI-5.7 | 标题必填 | 不输入标题 → 提交 disabled |
| MVP-UI-5.8 | 取消关闭 | 取消 → Dialog关闭 |

### AddMemberDialog

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-5.9 | 全量用户列表 | Dialog打开 → 显示所有 active 用户 |
| MVP-UI-5.10 | 搜索动态过滤 | 输入关键词 → 列表只显示匹配用户 |
| MVP-UI-5.11 | 选择用户+角色 | 选用户 → 选角色 → 提交 → `{ user_id, role }` |
| MVP-UI-5.12 | 角色默认值 | 角色dropdown默认选中 "member" |

### AddMemberDialog异常

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-5.13 | 搜索无结果 | 输入不匹配用户名 → 空列表+"无匹配用户"提示 |
| MVP-UI-5.14 | 未选用户提交 | 不选用户 → 提交 disabled |
| MVP-UI-5.15 | API返回500 | mock fetch 500 → Dialog显示错误，不关闭 |
| MVP-UI-5.16 | 搜索API失败 | 搜索接口失败 → "加载失败"提示，全量列表仍可用 |
| MVP-UI-5.17 | 添加已存在成员 | 后端409 → Dialog显示错误提示 |

### RegisterAgentDialog

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-5.18 | 正常注册 | 选类型→选模型→输入名称→提交 → `{ agent_type, model_id, display_name }` |
| MVP-UI-5.19 | 类型单选 | 只能选择一种类型，不能多选 |
| MVP-UI-5.20 | 模型下拉选项 | 包含 claude-haiku-4-5-20251001 和 claude-sonnet-4-6 |
| MVP-UI-5.21 | display_name预填 | 选summary → 预填"总结Agent" |

### RegisterAgentDialog异常

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-5.22 | 不选类型提交 | → 提交 disabled |
| MVP-UI-5.23 | 注册API失败 | mock 500 → 显示错误，不关闭 |
| MVP-UI-5.24 | 空名称提交 | 清空预填 → 提交 disabled |

测试文件：`web/src/__tests__/Dialogs.test.tsx` 新增

---

## 步骤6：ProjectTree与Dialog联动 — Vitest+手动

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-6.1 | 图标按钮→Dialog打开 | ➕按钮 → CreateThreadDialog状态变为open |
| MVP-UI-6.2 | Dialog提交→SSE更新 | 提交 → apiPost → mock SSE推送 → state更新 |

手动清单：➕→线程出现；👤→成员出现；🤖→Agent出现

---

## 步骤7：IM泡泡讨论面板 — Vitest

### MessageBubble

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-7.1 | 自己右对齐蓝泡泡 | participant_id=currentUserId → 右对齐+蓝色+头像右侧 |
| MVP-UI-7.2 | 别人左对齐浅泡泡 | ≠current + human → 左对齐+浅色+头像左侧 |
| MVP-UI-7.3 | Agent浅蓝泡泡🤖 | agent → 左对齐+浅蓝+蓝色边框+🤖图标 |
| MVP-UI-7.4 | 头像首字母 | display_name="张三" → "张" |
| MVP-UI-7.5 | 泡泡max-width | max-width: 70% |

### DiscussionPanel斜杠命令

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-7.6 | /summarize发送 | 输入 `/summarize`+Enter → `{ request_summary: true }` |
| MVP-UI-7.7 | /summarize带内容 | `/summarize 观点` → `{ request_summary: true, content: '观点' }` |
| MVP-UI-7.8 | Enter发送 | 普通消息+Enter → 发送，textarea清空 |
| MVP-UI-7.9 | Shift+Enter换行 | Shift+Enter → 换行，不发送 |
| MVP-UI-7.10 | 空消息不发送 | 空+Enter → 不触发 |
| MVP-UI-7.11 | /help显示帮助 | `/help` → 显示帮助提示，不发送 |

### DiscussionPanel异常

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-7.12 | 发送失败回滚 | apiPost 500 → 错误提示，消息不从列表移除（乐观更新回滚） |
| MVP-UI-7.13 | SSE去重 | POST返回+SSE推送同一消息 → 列表不重复 |
| MVP-UI-7.14 | SSE断连后重连推送 | mock EventSource error事件→断连→重新创建SSE连接→新消息通过重连后的SSE正常推送 |
| MVP-UI-7.15 | 超长消息 | >10000字符 → 提交disabled或截断提示 |
| MVP-UI-7.16 | 空消息列表 | `messages=[]` → "暂无消息" |

### MVP-20迁移

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-20.2-更新 | MessageItem→MessageBubble | human/agent样式区分 |
| MVP-20.3-更新 | 请求总结→斜杠命令 | MVP-UI-7.6替代 |

---

## 步骤8：产出卡片+Agent状态卡片 — Vitest

### OutputCard

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-8.1 | generated | Badge"待审批" + diff渲染 + 批准/修改按钮 |
| MVP-UI-8.2 | 批准产出 | 点击 → `onApproveOutput(output_id, feedback)` |
| MVP-UI-8.3 | 要求修改 | 展开→输入问题描述+建议→提交 → `onRequestRevision` |
| MVP-UI-8.4 | approved | Badge"已批准" + "已自动commit"提示 + 无按钮 |
| MVP-UI-8.5 | revision_requested | Badge"待修改" + 显示修改要求 |
| MVP-UI-8.6 | diff渲染 | react-diff-viewer-continued：增行绿色、删行红色 |

### OutputCard异常

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-8.7 | 批准API失败 | mock 500 → 错误提示，按钮可重试 |
| MVP-UI-8.8 | 修改未填问题 | 问题输入空 → 提交 disabled |
| MVP-UI-8.9 | diff=null | → "无diff内容"提示 |
| MVP-UI-8.10 | diff="" | 同上 |

### AgentStatusCard

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-8.11 | idle | 绿色/灰色边框+"空闲" |
| MVP-UI-8.12 | running | 蓝色边框+"工作中"+current_task（后端API返回running，前端UI文案映射为"工作中"） |
| MVP-UI-8.13 | error | 红色边框+"错误" |
| MVP-UI-8.14 | Agent类型 | 显示 summary/decompose/execute |

### MVP-20迁移

MVP-20.5 OutputDiff→OutputCard内嵌diff

---

## 步骤9：三tab执行面板+Workspace集成 — Vitest+手动

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-9.1 | 三tab切换 | 计划/产出/Agent Tab点击→内容切换 |
| MVP-UI-9.2 | 计划按状态分组 | proposed→待审批组；approved→已批准组；rejected→已拒绝组 |
| MVP-UI-9.3 | 产出按状态分组 | generated→待审批组；revision_requested→待修改组；approved→已批准组 |
| MVP-UI-9.4 | 拒绝计划交互 | proposed计划→点击拒绝→展开reason输入框→输入原因→提交→`onRejectPlan(plan_id, reason)` |
| MVP-UI-9.5 | 拒绝计划未填原因 | reason输入空→提交disabled |
| MVP-UI-9.6 | 拒绝计划API失败 | mock apiPost 500→错误提示，不关闭展开区 |

手动清单：三栏可拖拽、左栏可折叠、各Dialog触发

---

## 步骤10：测试更新+质量门禁

| 用例 | 描述 | 验证 |
|------|------|------|
| MVP-UI-10.1 | make check全量通过 | format+lint+type+test-all+test-front+audit |
| MVP-UI-10.2 | 组件引用更新 | MessageItem→MessageBubble、ThreadList→ProjectTree、OutputDiff→OutputCard |

---

## E2E测试（Playwright）— 核心循环完整验证

`tests/e2e/test_core_loop.py`

> **Agent产出策略**：E2E中Agent（总结/分解/执行）的产出通过mock ModelAdapter触发，不消耗真实API token。后端启动时注入StubAdapter，E2E测试通过API注入预设的Agent产出（摘要、计划、代码diff），确保E2E-5/6/7可稳定执行。

### 正常流程

| 用例 | 描述 | 验证 |
|------|------|------|
| E2E-1 | 注册+登录+创建项目 | 第一个用户→自动审批→登录→创建项目→ProjectTree出现项目节点 |
| E2E-2 | 注册Agent | 🤖按钮→选类型+模型+名称→Agent Tab出现卡片 |
| E2E-3 | 添加成员 | 第二用户注册+审批→👤→搜索+选角色→验证成员添加 |
| E2E-4 | 创建线程+发消息 | 点击项目→输入消息→中栏显示蓝色泡泡 |
| E2E-5 | /summarize请求总结 | `/summarize`+Enter → Agent产出浅蓝泡泡 |
| E2E-6 | 审批执行计划 | 计划Tab→批准→状态approved |
| E2E-7 | 审批产出 | 产出Tab→查看diff→批准→显示"已自动commit" |
| E2E-8 | SSE实时更新 | 两个窗口→一个发消息→另一个自动看到 |

### 异常流程

| 用例 | 描述 | 验证 |
|------|------|------|
| E2E-9 | 登出后操作 | 登出→发消息→重定向登录页 |
| E2E-10 | 空项目状态 | 非项目成员→工作区空状态 |
| E2E-11 | 拒绝计划 | 拒绝+输入原因→状态rejected |
| E2E-12 | 要求修改产出 | 要求修改+输入问题→状态revision_requested |
| E2E-13 | JWT过期 | 过期→操作→重定向登录页 |
| E2E-14 | 网络断开恢复 | 断网→失败→恢复→重试成功 |
| E2E-15 | 左栏折叠/展开 | 折叠→中栏变宽→展开→恢复 |

---

## 测试文件清单

| 步骤 | 测试文件 | 新增/修改 |
|------|---------|----------|
| 1 | `tests/integration/test_auth_api.py` | 修改（+TestUserListSearch） |
| 3 | `web/src/__tests__/api.test.ts` | 修改（+apiGet params） |
| 3 | `web/src/__tests__/slashCommands.test.ts` | 新增 |
| 3 | `web/src/__tests__/Workspace.test.tsx` | 修改（+MVP-UI-3.10） |
| 4-9 | `web/src/__tests__/Workspace.test.tsx` | 修改（+MVP-UI-4.x~9.x，更新MVP-20.x） |
| 5 | `web/src/__tests__/Dialogs.test.tsx` | 新增 |
| 10 | `make check` | 全量验证 |
| E2E | `tests/e2e/test_core_loop.py` | 新增 |